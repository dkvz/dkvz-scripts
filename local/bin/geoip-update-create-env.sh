#!/bin/bash

# Create the config file with environment variables that my GeoIP update script uses.
# The API key is somewhat secret but I won't bother to encrypt it.
# Otherwise, 7z could be used for that (openssl too).